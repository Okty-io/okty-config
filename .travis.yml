language: python
python: "3.6"

cache: pip

install:
    - pip install awscli
    - export PATH=$PATH:$HOME/.local/bin

script:
    - ./deploy/build.sh

deploy:
    provider: script
    script: ./deploy/upload.sh
    on:
        branch: dev

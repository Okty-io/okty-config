language: node_js
node_js: "8.10"

script:
    - npm install

before_deploy:
    - apt-get install python-pip
    - pip install awscli
    - export PATH=$PATH:$HOME/.local/bin

deploy:
    provider: script
    script: ./deploy/build.sh
    on:
        branch: dev

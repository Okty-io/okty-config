language: python
python: "3.6"

cache: pip

install:
    - pip install awscli
    - export PATH=$PATH:$HOME/.local/bin

deploy:
    provider: script
    script: ./deploy/build.sh
    on:
        branch: dev

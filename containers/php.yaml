name: "PHP FPM"
image: "https://cdn.worldvectorlogo.com/logos/php-1.svg"
version: 7.2-fpm
image: php

environments:

ports:
 
volumes:
  fields:
    "/var/www/html": "Web Folder (Same as web server folder)"
    "container_name": "Container name"
  defaults:
    "/var/www/html": "./"s
    "container_name": "octy_php"

extensions:

server {
    resolver 127.0.0.11 ipv6=off;

    listen       80;
    server_name  _;

    root /usr/share/nginx/html/{{root_folder}};
    index index.php index.html;

    {{map_upload_size}}

    location / {
        try_files $uri /index.php$is_args$args;
    }

    {{php_container_link}}
}
